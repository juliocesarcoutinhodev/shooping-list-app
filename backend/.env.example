# ==========================================
# SHOPPING LIST API - Environment Variables
# ==========================================
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Substitua os valores de exemplo por valores reais
# 3. NUNCA commite o arquivo .env no repositório
#
# Gerar JWT_SECRET seguro:
# openssl rand -base64 32
# ==========================================

# MySQL Database Configuration
MYSQL_ROOT_PASSWORD=root_password
MYSQL_DATABASE=shoppinglist_db
MYSQL_USER=admin
MYSQL_PASSWORD=admin
MYSQL_PORT=3306
MYSQL_HOST=localhost

# JWT Configuration (OBRIGATÓRIO - Mínimo 32 caracteres / 256 bits)
# Gere com: openssl rand -base64 32
JWT_SECRET=SUBSTITUA-POR-UM-SECRET-GERADO-COM-OPENSSL-RAND-BASE64-32
JWT_ISSUER=shopping-list-api

# Application Configuration
APP_NAME=shopping-list
PROFILE=dev

